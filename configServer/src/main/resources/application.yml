server:
  port: 8000
spring:
  application:
    name: config server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alsgh9948/spring_cloud_study/tree/main/config

management:
  endpoints:
    web:
      exposure:
        include: "*"
