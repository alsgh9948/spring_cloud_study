info:
  description: "server default application.yml"

test:
  text: "default"

management:
  endpoints:
    web:
      exposure:
        include: refresh,info

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8005/eureka/